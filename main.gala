extern fn free(ptr: rawptr);
extern fn calloc(n:int, size: int): rawptr;
extern fn memcpy(dst: rawptr, src:rawptr, size:int);
struct v2 {
    x: flt, y: flt
}
fn main(): int {
    i := 10.2;
    buf := [1024]byte{};
    b(&buf);
    return transmute(int)i;
}
fn b(buf: *[1024]byte) {
    return ;
}
