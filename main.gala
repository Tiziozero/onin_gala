extern fn printf(fmt: ^byte, data:..any);

fn print_i32(fmt: string, data: ..i32) {
    i := 0;
    while i < len(data) {
        printf(&fmt[0], data[i]);
    }
    return;
}

fn main() {
    print_i32("Hello, %d.\n", 1, 2, 3, 4, 5);
    return;
}
